{
  "name": "hipaw-workbench",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node server/index.mjs",
    "test": "node --test tests/*.test.mjs",
    "build:canvas": "npm --prefix vendor/infinite-canvas/web run build",
    "build:pages": "node scripts/build-pages.mjs"
  },
  "engines": {
    "node": ">=22"
  }
}
